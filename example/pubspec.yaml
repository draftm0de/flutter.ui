name: draftmode_ui_example
description: Example app for DraftMode Worker plugin.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: "3.35.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  draftmode_ui:
    path: ..
#    git: https://github.com/draftm0de/flutter.ui
#    version: ^1.0.0

  draftmode_localization:
    git: https://github.com/draftm0de/flutter.localization
    version: ^1.0.0

# for the example
  flutter_platform_widgets: ^9.0.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
